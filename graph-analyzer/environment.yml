name: graph-analyzer
channels:
  - intel
  - rapidsai
  - nvidia
  - numba
  - conda-forge
  - defaults
dependencies:
  - python>=3.7
  - scipy
  - rapids>=0.14,<0.20
  - cudatoolkit>=10,<12
  - protobuf
  - pygraphviz
  - pip
  - pip:
      - tld
prefix: ./envs
